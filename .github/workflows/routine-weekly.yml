name: Routine — Weekly KPI Snapshot (Mon 7:08 PM CT)

on:
  schedule:
    # Monday 7:08 PM CST = Tuesday 01:08 UTC  |  shifts to 00:08 UTC in summer (CDT)
    - cron: '8 1 * * 2'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  kpi-snapshot:
    name: KPI Snapshot
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/smilehaus/rcm-dashboard.git

      - name: fetch-kpi-snapshot
        run: npx tsx scripts/fetch-kpi-snapshot.ts
        env:
          CLICKUP_API_TOKEN: ${{ secrets.CLICKUP_API_TOKEN }}
